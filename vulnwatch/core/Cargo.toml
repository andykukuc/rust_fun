[package]
name = "vulnwatch-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure domain logic for Rust Vuln Watch: feed parsing, version comparison, advisory matching. No I/O."

[lib]
name = "vulnwatch_core"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
percent-encoding = { workspace = true }
