[package]
name = "vulnwatch-worker"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cloudflare Worker: feed sync, inventory ingest, matching and API for Rust Vuln Watch."

[lib]
crate-type = ["cdylib"]

[dependencies]
vulnwatch-core = { path = "../core" }
worker = "0.8"
